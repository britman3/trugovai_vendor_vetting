// Prisma client placeholder
// The actual Prisma client will be generated when prisma generate is run
// For now, we're using mock data in mockData.ts

// This file will be properly implemented once the database is configured
// To set up:
// 1. Ensure DATABASE_URL is set in .env
// 2. Run: npx prisma generate
// 3. Run: npx prisma db push

// Placeholder export to prevent import errors
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export const prisma: any = null;
export default prisma;
